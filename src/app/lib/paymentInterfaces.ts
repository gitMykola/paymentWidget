export interface PaymentMethod {
  id: string;
  name: string;
  img_url: string;
}
export interface Country {
  code: string;
  name: string;
}
export interface Position {
  lat: string;
  lng: string;
}
